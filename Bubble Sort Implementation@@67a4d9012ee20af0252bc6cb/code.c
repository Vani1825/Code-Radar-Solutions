#include <stdio.h>
int bubblesort(arr,n){
for(inti=0;i<=)
}
int printArray(arr,n){

}
int main(){
    int n;
    scanf("%d",&n);
    int arr[n];
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    bubblesort(arr,n);
    printArray(arr,n);
}